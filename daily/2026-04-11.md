- CashPilot ship-scope stayed locked to bugfix-only on restored baseline `fe8d6a7`: manual transaction entry, Pilot clear/new chat, and history replay/dedup fix. Justin explicitly did not want Auditor/Luna involved for this pass, only Bolt coding with parent orchestration.
- Bolt repeatedly targeted the wrong folder (`CashPilot_before_restore`) for manual-entry and dedup attempts, so those outputs were treated as non-shippable even when the ideas were useful. Justin moved that directory to try to stop Bolt from drifting there.
- Useful recovered direction from failed Bolt passes: manual transaction entry should live in Activity with a `+` sheet flow reusing the existing transaction edit UI; dedup fix concept was to preserve conversational text in history but strip already-executed tool/action payloads from assistant history to prevent replay.
- Direct live-app implementation was used instead of more Bolt retries for urgent scope. Claimed completed work in live app: Activity got manual add transaction via `+` and empty-state CTA, `EditTransactionSheet` was adapted to support both create and edit, and Pilot got a three-dot menu with `New Chat` that clears history and restores welcome state.
- Real build verification was reported as successful after the direct live-app changes (`xcodebuild ... build` -> `** BUILD SUCCEEDED **`). No commit was made yet because the repo had unrelated dirty files and the goal was to avoid bundling noise into the fix.
- CashPilot follow-up ship work on 2026-04-11 added real premium-gated polish around the restored baseline: Dashboard Reports now gates before navigation, Insights are fully Pro-only to match paywall copy, and `EditTransactionSheet` receipt access now opens paywall for free users instead of leaking through.
- Pilot UX was upgraded from the earlier hidden/three-dot action to visible top controls: a clear `New Chat` button and a real `Chat History` transcript view backed by the persisted single-thread chat history in SwiftData/`AgentService` (not fake multi-chat support).
- Manual transaction entry now supports receipt OCR autofill using the app's existing `PhotoImportService`: scanning/importing a receipt can populate amount, merchant, category, and date automatically, with highlighted upsell copy plus a Pro crown badge in the receipt section.
- Activity page also got a top camera entry point, modeled after Budget: Pro users can take a photo or choose from library, run OCR, and then land in the manual transaction sheet with fields prefilled from the scanned receipt.
- Justin explicitly disliked silence during long-running work ('you went quiet. what happened. i don't like when you go quiet...'). Important interpersonal reminder: during builds or multi-step edits, send a brief progress update instead of going dark.
