- CashPilot receipt import fixes landed in the Activity tab flow.
- Replaced the bad library picker behavior with the normal image picker in EditTransactionSheet, so choosing from library no longer shows the awkward full-screen "Select Photo" intermediary.
- Fixed Activity tab receipt-import handoff timing so after choosing a receipt photo, the picker dismisses cleanly and then opens the prefilled New Transaction sheet.
- Added visible processing feedback/spinner for the Activity camera/library receipt scan flow because OCR processing had a silent pause that felt broken.
- Fixed receipt parsing logic in PhotoImportService so explicit receipt totals are preferred over summing detected amounts, preventing double-counting tax. Example bug: sample receipt total should be 84.80, not 92.80.
- Receipt autofill now uses a best receipt transaction candidate instead of blindly taking the first OCR expense line item.
- CashPilot iOS simulator builds succeeded after the fixes.
- CashPilot receipt OCR bug root cause was upstream in the Vercel OCR endpoint prompt (`apps/CashPilot/vercel/api/cashpilot-ocr.js`): it explicitly told the model to skip totals/subtotals/grand totals, which forced the iOS app to reconstruct receipt totals from incomplete line-item data.
- Implemented the proper fix at the parser source: updated the OCR prompt/response shape to preserve receipt-level fields (merchant, subtotal, tax, final total, short note) and updated `CashPilot/Services/PhotoImportService.swift` to prefer structured receipt summary totals before falling back to heuristics.
- Verified CashPilot simulator build success after the parser-source fix and committed the work with git commit `6b9536e` (`Fix receipt OCR totals at the parser source`).
- Fixed Vercel endpoint deployment blocker: vercel.json was locked/corrupted ("Unknown system error -11"); restored proper Astro Vercel config and redeployed. Endpoint now healthy (`GET https://justinbundrick.dev/api/cashpilot-ocr` returns status ok).
- Added robust fallback parsing in PhotoImportService for receipts lacking structured `receipt_summary`: `parseTotalFromSummary` extracts dollar amounts from summary prose (e.g., "$957.14") and `parseMerchantFromSummary` extracts vendor names (e.g., "Quest Provider").
- Re-ordered receipt total resolution to check parsed summary total before explicit line items, preventing double-counting and matching holistic LLM interpretation.
- Swift build succeeded after fixing variable scoping and helper placement; committed as `7e29229` on Ledgr (CashPilot repo). Changes ensure images like the Quest Provider test photo autofill merchant + total correctly even when `receipt_summary` is missing.
- CashPilot receipt import fixes landed in the Activity tab flow.
- Replaced the bad library picker behavior with the normal image picker in EditTransactionSheet, so choosing from library no longer shows the awkward full-screen "Select Photo" intermediary.
- Fixed Activity tab receipt-import handoff timing so after choosing a receipt photo, the picker dismisses cleanly and then opens the prefilled New Transaction sheet.
- Added visible processing feedback/spinner for the Activity camera/library receipt scan flow because OCR processing had a silent pause that felt broken.
- Fixed receipt parsing logic in PhotoImportService so explicit receipt totals are preferred over summing detected amounts, preventing double-counting tax. Example bug: sample receipt total should be 84.80, not 92.80.
- Receipt autofill now uses a best receipt transaction candidate instead of blindly taking the first OCR expense line item.
- CashPilot iOS simulator builds succeeded after the fixes.
- CashPilot receipt OCR bug root cause was upstream in the Vercel OCR endpoint prompt (`apps/CashPilot/vercel/api/cashpilot-ocr.js`): it explicitly told the model to skip totals/subtotals/grand totals, which forced the iOS app to reconstruct receipt totals from incomplete line-item data.
- Implemented the proper fix at the parser source: updated the OCR prompt/response shape to preserve receipt-level fields (merchant, subtotal, tax, final total, short note) and updated `CashPilot/Services/PhotoImportService.swift` to prefer structured receipt summary totals before falling back to heuristics.
- Verified CashPilot simulator build success after the parser-source fix and committed the work with git commit `6b9536e` (`Fix receipt OCR totals at the parser source`).
- Fixed Vercel endpoint deployment blocker: vercel.json was locked/corrupted ("Unknown system error -11"); restored proper Astro Vercel config and redeployed. Endpoint now healthy (`GET https://justinbundrick.dev/api/cashpilot-ocr` returns status ok).
- Added robust fallback parsing in PhotoImportService for receipts lacking structured `receipt_summary`: `parseTotalFromSummary` extracts dollar amounts from summary prose (e.g., "$957.14") and `parseMerchantFromSummary` extracts vendor names (e.g., "Quest Provider").
- Re-ordered receipt total resolution to check parsed summary total before explicit line items, preventing double-counting and matching holistic LLM interpretation.
- Swift build succeeded after fixing variable scoping and helper placement; committed as `7e29229` on Ledgr (CashPilot repo). Changes ensure images like the Quest Provider test photo autofill merchant + total correctly even when `receipt_summary` is missing.
- Fixed Swift regex escape sequence bug: patterns in `parseTotalFromSummary` were using single-escaped `\.` instead of Swift-required `\\.`, causing "invalid escape sequence" compiler errors. Rewrote all 5 pattern strings and the fallback pattern to use double-escaped dots, rebuilt successfully. Commit pushed to Ledgr.
- Built and pushed completed receipt fix to Vercel endpoint (commit `726c6dc`). Endpoint confirmed healthy. Receipt totals now parse correctly at the source — `receipt_summary` with `total`, `subtotal`, `tax`, `merchant` is returned alongside line items.
- Added `query_transactions` tool to CashPilot agent:
  - Agent endpoint (`justinbundrick.dev/api/cashpilot-agent`) declares the tool and routes to `handleAgentQuery` which runs SQL against the local SQLite DB via Prisma, supporting filters: merchant (LIKE), category (LIKE), date ranges (relative like "today"/"yesterday"/"this week"/"this month" or explicit YYYY-MM-DD), and amount comparisons (>, <, =). Returns formatted text summary with matching transaction count, total sum, and a 5-item preview list.
  - iOS `AgentService` now includes `"query_transactions"` in `allowedTools` and executes `handleQueryTransactions` which calls the endpoint, parses the JSON response, and returns a natural-language answer string.
  - This enables users to ask the PilotBar questions like "how much was spent at Target today" or "total for this month" and get instant answers from the transaction database.
- Cleaned up TransactionListView to remove the old manual text search — deleted the searchText state and the searchBar view; the activity screen now uses PilotBar as the sole query input and relies on agent-powered data queries instead of local filtering. Filter chips and sort control remain for UI-based filtering/sorting.
