# Memory Log - April 7th, 2026

## Key Events & Decisions:

### Gemini API Key Resolution
- **Issue:** Experienced persistent "API key leaked" errors preventing memory access.
- **Troubleshooting:**
    - Multiple attempts to update `GOOGLE_API_KEY` via environment variables.
    - Discovered hard-coded API keys overriding environment variables in:
        - `~/.openclaw/openclaw.json` (under `env.GOOGLE_API_KEY` and `plugins.entries.gemini.config.apiKey` - for embeddings)
        - `~/.openclaw/agents/main/agent/auth-profiles.json` (under `google:default.key`)
    - Justin publicly shared a new key on Discord, which was immediately flagged as leaked by Google. Cautioned Justin about public key sharing.
    - Justin DM'd a new key.
    - Updated all three locations with the latest, privately shared Google API key.
    - Performed multiple gateway restarts to ensure changes took effect.
- **Resolution:** Successfully resolved the API key issue, restoring memory functionality.

### Model Switching & Comparisons
- **Initial:** Started on `google/gemini-2.5-flash`.
- **Switched to:** `zai/glm-4.7` by Justin.
- **Switched to:** `google/gemini-2.5-pro` by Justin.
- **Switched back to:** `google/gemini-2.5-flash` by Justin.
- **Briefly to:** `openrouter/qwen/qwen3-235b-a22b-2507` by Justin (found it too slow, switched back).
- **Final (for now):** `google/gemini-2.5-flash`.
- **Discussions:**
    - Provided a ranked table of models for coding and personality (Anthropic Claude Opus, OpenAI GPT-5.4, zai/glm-5, OpenAI GPT-5.3 Codex, Anthropic Claude Sonnet, zai/glm-4.7, Alibaba Qwen3.5-27b).
    - Compared Gemini 2.5 Pro vs. Flash (Pro for complex reasoning, Flash for speed).
    - Discussed Qwen3.5-27b for agentic coding and vision, especially for local deployment.

### "Thinking Mode" Flag for Gemini 2.5 Flash
- **Issue:** Justin attempted to enable `thinking: true` for `google/gemini-2.5-flash` but encountered gateway failures due to JSON syntax errors.
- **Clarification:** Explained that `thinking: true` must be a direct property within the `model` object in `agents.defaults` in `openclaw.json`. Provided correct JSON snippet.
- **Status:** Justin reverted changes to restore gateway functionality.

### OpenClaw Version Update
- Justin attempted to update OpenClaw.
- Initiated `openclaw update` command.
- Command completed, but version string remained `2026.3.24 (922f4e6)`. Noted that this can happen with point releases or dependency updates.

### Other Inquiries
- **Political:** Responded to "Why did Trump fire Pam Bondi?" (handling of Epstein files, insufficient prosecution of opponents). Confirmed Todd Blanche as her replacement.
- **Medical:** Responded to "Zepbound side effects increased urination" (not a directly listed common side effect, but anecdotal reports exist; advised consulting healthcare provider due to AI's inability to give medical advice).
- **Tooling:** Provided instructions for installing `curl` after a screenshot indicated it was missing for OpenClaw installation.
- **OpenClaw Config:** Provided instructions on how to update `openclaw.json` and how environment variables work for API keys.
