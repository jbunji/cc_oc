# 2026-04-09

- Verified Bolt sub-agent model configuration by reading `/Users/openclaw/.openclaw/openclaw.json`: **Bolt (and Luna/Sage/Nova) `model.primary` is `google/gemini-2.5-flash`**.
- Prior confusion came from observing the *main* chat session's model (OpenRouter) while there was **no active Bolt sub-agent session**, so Bolt's model wouldn't appear in `session_status` until a Bolt run was started.
- To validate a Bolt model change: kick off a Bolt sub-agent run, then check the relevant sub-agent/session status.

- Debugged CashPilot spreadsheet OCR issue: the current OCR endpoint (`vercel/api/cashpilot-ocr.js`) returns only `transactions`, and its rules to skip totals/subtotals often causes spreadsheets that are actually **category budget allocations** (Week 1-Week 5 + Total) to produce few/zero transaction items, so nothing appears in Activity.
- Determined expected handling for that spreadsheet format is likely **budgets import** (update `BudgetCategory.monthlyBudget`) rather than importing into dated `Transaction` rows, unless we add heuristics to infer dates.
- Confirmed Activity/Transactions now has a non-chat manual entry path via a new `AddTransactionSheet` and an Activity tab "+ Add Transaction" button (Pilot remains the smart/chat mode).

## CashPilot "old app" appearance issue (Apr 9 ~10:30 AM)
- jbunjii running app on **real iPhone** (not simulator), saw old CashPilot branding/icon instead of Ledgr rebrand.
- Git diff confirmed only `TransactionListView.swift` (+301 lines) and `EditTransactionSheet.swift` (+50/-2 lines) changed — no UI rollback from our side.
- Two xcodeproj files exist: `CashPilot.xcodeproj` and `CashPilot 2.xcodeproj` — both have same scheme name "CashPilot". This is a likely source of confusion when running on device.
- **Unresolved**: did not confirm which xcodeproj jbunjii is using in Xcode when pressing Run. Old icon/name likely means wrong project is being built and installed.
- Next step: confirm jbunjii deleted app from iPhone, selected correct device in Xcode, used correct project, and ran clean build.
- Running on Claude Sonnet 4.6 via OpenRouter.